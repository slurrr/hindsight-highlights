# hindsight-highlights

Local ops repo for running Hindsight as a first-class service (not embedded).

- Planning doc: `docs/PLAN.md`
- Service env: `env/hindsight.env`
- systemd (user) units: `systemd/`
- Utility scripts: `scripts/`
