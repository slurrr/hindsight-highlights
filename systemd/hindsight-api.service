# Deprecated placeholder: do not install this as a system service.
#
# Hindsight on this workstation is intentionally managed only as a manual
# systemd --user service from:
#   systemd/user/hindsight-api.service
#
# Postgres remains the only system-level service dependency.
# The previously installed /etc/systemd/system/hindsight-api.service was removed
# during the 2026-06-11 service audit to avoid duplicate API processes and port
# contention on 127.0.0.1:8888.
